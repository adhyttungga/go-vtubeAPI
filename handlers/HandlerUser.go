package handlers

import (
	"net/http"
)

func RegisterUser(w http.ResponseWriter, r *http.Request) {
	// var payload, user structs.User

	// if err := json.NewEncoder(r.Body).Decode(&payload); err != nil {
	// 	http.Error(w, err.Error(), http.StatusInternalServerError)
	// 	return
	// }
	
}